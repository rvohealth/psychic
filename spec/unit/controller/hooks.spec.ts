import background from '../../../src/background'
import BackgroundTestController from '../../../test-app/app/controllers/BackgroundTestController'

describe('PsychicController hooks', () => {
  describe('before', () => {
    it('calls before actions before running a method', async () => {})

    context('only filter is applied', () => {
      it('calls before action when method is called', async () => {})

      it('does not call before action when a different method is called', async () => {})
    })
  })
})
