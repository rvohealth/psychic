import PsychicRouter from '../../../src/router/index.js'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function internalRoutes(r: PsychicRouter) {
  // add routes here, perhaps by running `pnpm psy g:resource internal/pets Pet name:citext birthdate:date species:enum:pet_species:dog,cat,fish`
}
